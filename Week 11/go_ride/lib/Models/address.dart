class Address
{
  dynamic placeFormattedAddress;
  dynamic placeName;
  dynamic PlaceId;
  dynamic latitude;
  dynamic longitude;

  Address( { this.placeFormattedAddress, this.longitude, this.latitude, this.placeName, this.PlaceId});

}